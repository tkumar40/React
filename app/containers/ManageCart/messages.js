/*
 * ManageCart Messages
 *
 * This contains all the text for the ManageCart container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageCart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageCart container!',
  },
});
