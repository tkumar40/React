/*
 * MianContainer Messages
 *
 * This contains all the text for the MianContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MianContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MianContainer container!',
  },
});
